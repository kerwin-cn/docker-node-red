services:
  node-red:
    build:
      context: ./node-red
    environment:
      - TZ=Asia/Shanghai
    ports:
      - "1880:1880"
    user: "1000:1000"
    networks:
      - node-red-net
    volumes:
      - ./data:/data

volumes:
  data:
networks:
  node-red-net:
